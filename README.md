# CLI Task Tracker

A simple Java command-line interface for managing tasks.

## Setup
1. Clone the repository.
2. Create a MySQL database using the `schema.sql` file provided.
3. Copy `.env.example` to a new file named `.env` and fill in your local DB credentials.
4. Build the project:
   ```bash
   mvn clean install
